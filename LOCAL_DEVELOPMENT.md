# 💻 Локальная разработка

Руководство по настройке локальной разработки для Telegram Cover Bot.

## 📋 Требования

- Python 3.8+
- Git
- VS Code (рекомендуется)
- SSH доступ к серверу (для деплоя)

### Windows
- PowerShell 5.0+
- OpenSSH Client (встроен в Windows 10/11)

### Linux/Mac
- Bash
- rsync

## 🚀 Первоначальная настройка

### 1. Клонирование репозитория

```bash
# Windows (PowerShell или Git Bash)
cd C:\Dev
git clone git@github.com:Supaple-x/telegram-cover-bot.git
cd telegram-cover-bot

# Linux/Mac
cd ~/Dev
git clone git@github.com:Supaple-x/telegram-cover-bot.git
cd telegram-cover-bot
```

### 2. Создание виртуального окружения

```bash
# Windows
python -m venv venv
.\venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

```bash
# Скопируйте пример файла
cp .env.example .env

# Отредактируйте .env и добавьте ваши токены
# Windows: notepad .env
# Linux/Mac: nano .env
```

### 5. Установка ffmpeg (для обработки аудио)

**Windows:**
1. Скачайте ffmpeg: https://ffmpeg.org/download.html
2. Распакуйте и добавьте в PATH

**Linux:**
```bash
sudo apt install ffmpeg
```

**Mac:**
```bash
brew install ffmpeg
```

## 🔧 Работа в VS Code

### Рекомендуемые расширения:
- Python (Microsoft)
- Pylance (Microsoft)
- GitLens (Eric Amodio)
- Better Comments

### Запуск бота локально

1. **Через терминал:**
```bash
python bot.py
```

2. **Через отладчик VS Code:**
   - Нажмите F5 или используйте панель Run & Debug
   - Выберите "Python: Bot"

## 📦 Деплой на сервер

### Автоматический деплой

**Windows (PowerShell):**
```powershell
# Деплой с перезапуском сервиса
.\deploy.ps1

# Деплой без перезапуска
.\deploy.ps1 -SkipRestart
```

**Linux/Mac:**
```bash
# Деплой с перезапуском сервиса
./deploy.sh

# Деплой без перезапуска
./deploy.sh --skip-restart
```

### Что делает скрипт деплоя:

1. Синхронизирует файлы на сервер (исключая .git, venv, logs и т.д.)
2. Перезапускает systemd сервис
3. Показывает статус сервиса

### Ручной деплой

Если нужно больше контроля:

```bash
# 1. Синхронизация файлов
scp -r ./* root@65.109.142.30:/opt/telegram-cover-bot/

# 2. Перезапуск сервиса
ssh root@65.109.142.30 "systemctl restart telegram-cover-bot"

# 3. Просмотр логов
ssh root@65.109.142.30 "journalctl -u telegram-cover-bot -f"
```

## 🔄 Git Workflow

### Базовый workflow:

```bash
# 1. Создайте новую ветку для фичи
git checkout -b feature/my-new-feature

# 2. Внесите изменения и закоммитьте
git add .
git commit -m "Add new feature"

# 3. Запушьте в GitHub
git push origin feature/my-new-feature

# 4. Создайте Pull Request на GitHub

# 5. После мержа в main, задеплойте на сервер
git checkout main
git pull
.\deploy.ps1  # или ./deploy.sh
```

### Быстрый hotfix:

```bash
# 1. Внесите изменения
git add .
git commit -m "Fix critical bug"

# 2. Запушьте в main
git push origin main

# 3. Деплой на сервер
.\deploy.ps1
```

## 🐛 Отладка

### Локальная отладка:

1. Установите breakpoint в VS Code (клик слева от номера строки)
2. Запустите отладчик (F5)
3. Бот остановится на breakpoint

### Отладка на сервере:

```bash
# Подключитесь к серверу
ssh root@65.109.142.30

# Просмотр логов в реальном времени
journalctl -u telegram-cover-bot -f

# Или файл логов
tail -f /opt/telegram-cover-bot/logs/bot.log

# Статус сервиса
systemctl status telegram-cover-bot

# Перезапуск
systemctl restart telegram-cover-bot
```

## 📝 Тестирование

### Запуск локально для тестов:

```bash
# Активируйте venv
source venv/bin/activate  # Linux/Mac
.\venv\Scripts\activate   # Windows

# Запустите бота
python bot.py
```

### Тестирование в Telegram:

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Проверьте функциональность

## 🔐 Безопасность

### ⚠️ Важно:

- **НИКОГДА** не коммитьте файл `.env` в git
- **НИКОГДА** не коммитьте `youtube_cookies.json` или `youtube_cookies.txt`
- Храните токены в безопасном месте
- Используйте разные токены для разработки и продакшена (если возможно)

### Проверка перед коммитом:

```bash
# Проверьте что будет закоммичено
git status

# Убедитесь что .env не в списке
git diff --cached
```

## 🛠️ Полезные команды

### Git:

```bash
# Посмотреть изменения
git status
git diff

# Отменить изменения в файле
git checkout -- filename

# Посмотреть историю
git log --oneline

# Синхронизировать с GitHub
git pull
```

### Python:

```bash
# Обновить зависимости
pip install -r requirements.txt --upgrade

# Добавить новую зависимость
pip install package-name
pip freeze > requirements.txt
```

## 📞 Сервер

**Адрес:** 65.109.142.30
**Пользователь:** root
**Путь:** /opt/telegram-cover-bot
**Сервис:** telegram-cover-bot.service

## ❓ Частые проблемы

### PowerShell не выполняет скрипты

```powershell
# Разрешите выполнение скриптов (от администратора)
Set-ExecutionPolicy RemoteSigned
```

### SSH не работает на Windows

```powershell
# Проверьте что OpenSSH установлен
Get-WindowsCapability -Online | Where-Object Name -like 'OpenSSH*'

# Установите если нужно
Add-WindowsCapability -Online -Name OpenSSH.Client~~~~0.0.1.0
```

### ffmpeg не найден

Убедитесь что ffmpeg в PATH:

```bash
# Проверка
ffmpeg -version

# Windows: добавьте путь к ffmpeg в System Environment Variables
# Linux/Mac: установите через пакетный менеджер
```

## 🎯 Следующие шаги

1. Клонируйте репозиторий локально
2. Настройте окружение
3. Запустите бота локально для проверки
4. Внесите первое изменение
5. Задеплойте на сервер

Удачной разработки! 🚀
