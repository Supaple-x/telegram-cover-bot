# 🎵 Telegram Cover Bot

Telegram бот для поиска и скачивания музыки из различных источников.

## 🎯 Возможности

- 🎬 Поиск в **YouTube**
- 🎵 Поиск в **YouTube Music**
- 🎶 Поиск в **VK Music** (в разработке)
- 🎧 Поиск в **Yandex Music** (в разработке)
- 🔊 Поиск в **SoundCloud**
- 📱 Удобный интерфейс с кнопками
- 📄 Постраничная навигация (10 треков на странице)
- ⚡ Быстрое скачивание и отправка аудио
- 🎧 Высокое качество аудио (MP3 320kbps)

## 🚀 Быстрый старт

### Требования
- Python 3.8+
- ffmpeg
- Telegram Bot Token
- YouTube Data API v3 Key (опционально)

### Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/YOUR_USERNAME/telegram-cover-bot.git
cd telegram-cover-bot
```

2. Создайте виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Настройте переменные окружения:
```bash
cp .env.example .env
nano .env
```

Заполните `.env`:
```
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
YOUTUBE_API_KEY=your_youtube_api_key
```

5. Запустите бота:
```bash
python bot.py
```

## 📖 Как использовать

1. Найдите бота в Telegram: `@your_bot_username`
2. Отправьте `/start` для начала
3. Введите название трека или исполнителя
4. Выберите источник для поиска
5. Нажмите на нужный трек для скачивания

### Примеры запросов:
- `Imagine Dragons Believer`
- `Coldplay`
- `The Beatles Yesterday`
- `Eminem Lose Yourself`

## 🔑 Получение API ключей

### Telegram Bot Token
1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

### YouTube Data API v3
1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект
3. Включите "YouTube Data API v3"
4. Создайте API ключ в разделе "Credentials"

## ⚙️ Развертывание на сервере

### Ubuntu/Debian

1. Подключитесь к серверу:
```bash
ssh root@your_server_ip
```

2. Установите зависимости:
```bash
apt update && apt upgrade -y
apt install python3 python3-pip python3-venv ffmpeg -y
```

3. Создайте проект:
```bash
mkdir -p /opt/telegram-cover-bot
cd /opt/telegram-cover-bot
```

4. Загрузите код и установите зависимости:
```bash
# Скопируйте файлы проекта
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

5. Настройте окружение:
```bash
nano .env
```

6. Настройте systemd:
```bash
cp systemd/telegram-cover-bot.service /etc/systemd/system/
systemctl daemon-reload
systemctl enable telegram-cover-bot
systemctl start telegram-cover-bot
```

7. Проверьте статус:
```bash
systemctl status telegram-cover-bot
journalctl -u telegram-cover-bot -f
```

## 📁 Структура проекта

```
telegram-cover-bot/
├── bot.py                      # Главный файл бота
├── config.py                   # Конфигурация и токены
├── requirements.txt            # Python зависимости
├── .env.example                # Пример файла окружения
├── .gitignore                  # Git ignore файл
├── README.md                   # Документация
├── LICENSE                     # Лицензия MIT
├── handlers/                   # Обработчики команд
│   ├── __init__.py
│   ├── start.py                # /start, /help, /about
│   ├── search.py               # Поиск музыки
│   └── download.py             # Скачивание треков
├── services/                   # Сервисы поиска
│   ├── __init__.py
│   ├── youtube_service.py      # YouTube/YT Music
│   ├── soundcloud_service.py   # SoundCloud
│   ├── vk_service.py           # VK Music (TODO)
│   └── yandex_service.py       # Yandex Music (TODO)
├── utils/                      # Утилиты
│   ├── __init__.py
│   ├── keyboards.py            # Клавиатуры
│   └── formatters.py           # Форматирование
├── logs/                       # Логи бота
├── downloads/                  # Временные файлы
└── systemd/                    # Systemd сервис
    └── telegram-cover-bot.service
```

## 🛠️ Управление сервисом

```bash
# Запуск
systemctl start telegram-cover-bot

# Остановка
systemctl stop telegram-cover-bot

# Перезапуск
systemctl restart telegram-cover-bot

# Статус
systemctl status telegram-cover-bot

# Логи
journalctl -u telegram-cover-bot -f
tail -f logs/bot.log
```

## 🔧 Конфигурация

Основные настройки в `config.py`:

- `MAX_RESULTS_PER_PAGE = 10` - Количество результатов на странице
- `MAX_FILE_SIZE_MB = 50` - Максимальный размер файла
- `AUDIO_FORMAT = 'mp3'` - Формат аудио
- `AUDIO_QUALITY = '320'` - Качество аудио

## 📊 Поддерживаемые источники

| Источник | Поиск | Скачивание | Статус |
|----------|-------|------------|--------|
| YouTube | ✅ | ✅ | Работает |
| YouTube Music | ✅ | ✅ | Работает |
| SoundCloud | ✅ | ✅ | Работает |
| VK Music | ❌ | ❌ | В разработке |
| Yandex Music | ❌ | ❌ | В разработке |

## 🚨 Ограничения

- Максимальный размер файла: 50 МБ (лимит Telegram)
- Формат аудио: MP3 (320 kbps)
- Максимум 10 результатов на странице
- YouTube API имеет дневные лимиты запросов

## 🐛 Устранение неполадок

### Бот не запускается
1. Проверьте токен в `.env` файле
2. Убедитесь что установлены все зависимости
3. Проверьте логи: `tail -f logs/bot.log`

### Ошибки скачивания
1. Убедитесь что установлен `ffmpeg`
2. Проверьте права доступа к папке `downloads/`
3. Некоторые видео могут быть заблокированы в вашем регионе

### YouTube API лимиты
1. Получите собственный API ключ
2. Или используйте только YouTube Music (не требует API ключа)

## 🤝 Вклад в проект

Pull requests приветствуются! Для больших изменений сначала откройте issue для обсуждения.

### Планы развития:
- [ ] Реализация VK Music
- [ ] Реализация Yandex Music
- [ ] Поддержка плейлистов
- [ ] Веб-интерфейс администратора
- [ ] База данных для статистики
- [ ] Docker контейнеризация

## 📄 Лицензия

MIT License - смотрите [LICENSE](LICENSE) для деталей

## 📧 Контакты

Если у вас есть вопросы или предложения, создайте issue в репозитории.

---

**⚠️ Дисклеймер:** Этот бот предназначен только для образовательных целей. Убедитесь, что вы соблюдаете авторские права и условия использования музыкальных платформ.
